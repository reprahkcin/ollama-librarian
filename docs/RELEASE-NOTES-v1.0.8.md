# Ollama Librarian v1.0.8

This release increments versioning after the Phase 0-6 quality rollout merge and ensures release documentation is published and discoverable on GitHub.

## Highlights

- Version bump to `v1.0.8` across package/runtime version markers.
- Added release notes artifact for `v1.0.8` in the repository.
- Published annotated Git tag `v1.0.8` with release summary.

## Versioning

- `pyproject.toml`: `1.0.8`
- `scripts/VERSION`: `v1.0.8`
- `src/ollama_librarian/VERSION`: `v1.0.8`

## Context

The preceding merged workstream completed response quality Phases 0-6, including:

- structured `/api/pdf/ask` contract and frontend rendering alignment
- retrieval/reranking calibration and traceability
- confidence badge calibration metadata/tests
- benchmark sign-off artifact runs
